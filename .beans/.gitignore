# Regenerable cache — rebuilt automatically by bn sync
index.yaml
archive.yaml

# File lock
index.lock

# Local agent run logs
agent_history.jsonl

# Archived beans (generated / internal)
archive/

# Local bean definitions (work tracking)
*.md
